# Internal scripts

These helpers are used by `../manage.sh`. Prefer that entrypoint:

```bash
./manage.sh                 # interactive menu
./manage.sh install
./manage.sh update
./manage.sh backup --dest /path
./manage.sh status
```
